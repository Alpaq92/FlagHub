name: Build & Release

# "Every merge to main triggers a rebuild and deploy" -
#   - On every push to main / master / fork-flagkit: build, test, optimise,
#     and upload the framework + asset archive as a workflow artefact
#   - On semver tags (v*.*.*): also publish a GitHub Release with the
#     archive attached

on:
  push:
    branches: [main, master, fork-flagkit]
    tags: ['v*.*.*']
    # Skip rebuild for docs-only changes. Tags still publish releases
    # regardless of which files changed in the tagged commit.
    paths-ignore:
      - '**.md'
      - 'docs/**'
      - 'LICENSE'
      - 'header.png'
      - 'docs/AUTOMATION.md'
      - 'SECURITY.md'
      - 'CHANGELOG.md'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  build:
    name: Build framework
    runs-on: macos-latest
    outputs:
      version: ${{ steps.meta.outputs.version }}
      archive: ${{ steps.archive.outputs.path }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Resolve version
        id: meta
        run: |
          if [[ "${GITHUB_REF}" == refs/tags/v* ]]; then
            echo "version=${GITHUB_REF#refs/tags/v}" >> "$GITHUB_OUTPUT"
          else
            echo "version=$(git rev-parse --short HEAD)" >> "$GITHUB_OUTPUT"
          fi

      - name: Build SPM
        run: swift build -c release

      - name: Test SPM
        run: swift test

      # SPM packages don't natively produce .framework bundles via
      # `xcodebuild archive`. The community tool swift-create-xcframework
      # generates a wrapper Xcode project, builds each platform slice as a
      # framework, and stitches them into an xcframework. Pinned to a
      # specific commit on the maintained segment-integrations fork for
      # reproducibility (upstream unsignedapps/swift-create-xcframework is
      # archived as of 2024).
      - name: Install swift-create-xcframework
        run: |
          set -e
          git clone https://github.com/segment-integrations/swift-create-xcframework.git /tmp/scx
          git -C /tmp/scx checkout 33079d3
          swift build --package-path /tmp/scx -c release
          sudo cp /tmp/scx/.build/release/swift-create-xcframework /usr/local/bin/

      - name: Build xcframework
        run: |
          set -e
          mkdir -p .build/xcframework
          swift-create-xcframework \
            --platform ios \
            --platform macos \
            --platform tvos \
            --stack-evolution \
            --output .build/xcframework \
            FlagHub
        continue-on-error: true

      # swift-create-xcframework's generated project omits the SPM resource
      # bundle target, so the produced .xcframework has the code but not
      # FlagHub.xcassets. Copy the bundle that `swift build -c release`
      # already produced into each framework slice. Safe to use one bundle
      # across platforms here because every imageset uses idiom=universal —
      # the compiled Assets.car is platform-independent. We remove the
      # framework signature after modification; downstream Xcode re-signs
      # on embed.
      - name: Inject resource bundle into xcframework slices
        run: |
          set -e
          XCFW=.build/xcframework/FlagHub.xcframework
          if [[ ! -d "$XCFW" ]]; then
            echo "::warning::xcframework not produced; skipping bundle injection"
            exit 0
          fi
          BUNDLE=$(find .build -type d -name FlagHub_FlagHub.bundle -not -path "*xcframework*" -not -path "*swift-create-xcframework*" | head -1)
          if [[ -z "$BUNDLE" ]]; then
            echo "::error::FlagHub_FlagHub.bundle not found in SPM build output"
            exit 1
          fi
          echo "Source bundle: $BUNDLE"
          for fw in "$XCFW"/*/FlagHub.framework; do
            slice=$(basename "$(dirname "$fw")")
            if [[ -d "$fw/Versions/A/Resources" ]]; then
              dest="$fw/Versions/A/Resources/"
            else
              dest="$fw/"
            fi
            echo "Injecting bundle into $slice -> $dest"
            cp -R "$BUNDLE" "$dest"
            rm -rf "$fw/_CodeSignature" "$fw/Versions/A/_CodeSignature" 2>/dev/null || true
          done
        continue-on-error: true

      - name: Archive
        id: archive
        run: |
          set -e
          mkdir -p dist
          tar -czf "dist/FlagHub-${{ steps.meta.outputs.version }}.tar.gz" \
            Sources/FlagHub \
            Assets \
            FlagHub.podspec \
            Package.swift \
            LICENSE \
            README.md \
            CHANGELOG.md \
            docs/OVERVIEW.md \
            header.png
          if [[ -d .build/xcframework/FlagHub.xcframework ]]; then
            tar -czf "dist/FlagHub-${{ steps.meta.outputs.version }}-xcframework.tar.gz" \
              -C .build/xcframework FlagHub.xcframework
          fi
          echo "path=dist" >> "$GITHUB_OUTPUT"

      - uses: actions/upload-artifact@v4
        with:
          name: FlagHub-${{ steps.meta.outputs.version }}
          path: dist/

  release:
    name: GitHub Release (tagged only)
    needs: build
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: FlagHub-${{ needs.build.outputs.version }}
          path: dist/
      - uses: softprops/action-gh-release@v2
        with:
          files: dist/*
          generate_release_notes: true
          fail_on_unmatched_files: true
